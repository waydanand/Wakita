import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
    return (
        <header>
            <h1>Welcome</h1>
            <p>Thank you for visiting our site. We hope that we can assist you with all your real estate queries and needs.</p>
        </header>
    );
};

Header.propTypes = {
    
};

export default Header;